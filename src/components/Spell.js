/*import React, { PropTypes } from 'react';

const Weapons = ({classNameObject, clickedSpell, hoverSpell, spellName}) => {


	return (
		<img 	className={classNameObject} 
				onClick={clickedSpell} 
				hoverSpell={hoverSpell}
				src={require('../../images/spells/'+{spellName}+'.png')} />
	);
};

Spell.propTypes = {
	classNameObject: PropTypes.object.isRequired,
	clickedSpell: PropTypes.func.isRequired,
	hoverSpell: PropTypes.func.isRequired,
	spellName: PropTypes.string.isRequired
};

export default Spell;

*/